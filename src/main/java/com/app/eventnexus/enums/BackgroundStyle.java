package com.app.eventnexus.enums;

/**
 * Visual background style hint used by the frontend ThemeService.
 * Each game genre maps to one of these styles to drive CSS variable injection.
 */
public enum BackgroundStyle {
    DARK,
    NEON,
    GRITTY,
    SLEEK,
    PIXEL,
    ARCANE
}
